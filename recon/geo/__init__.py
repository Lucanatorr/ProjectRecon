"""Geospatial as-built mapping (Development).

Field/desk-captured map geometry → billable quantities by contract code, emitted
as the same ``AsBuiltLine``s the reconciliation engine already consumes. Pure,
UI-free domain core. See docs/asbuilt_mapping_spec.md.
"""
